{
  "name": "openai-responses-anthropic-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone Anthropic-compatible proxy for OpenAI Responses API and OpenAI-compatible upstreams",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "start": "bun run src/cli.ts",
    "test": "bun test src/*.test.ts",
    "typecheck": "bun x tsc --noEmit",
    "build:binary": "mkdir -p dist && bun build --compile src/cli.ts --outfile dist/openai-responses-anthropic-proxy"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "bun": ">=1.3.0"
  }
}
